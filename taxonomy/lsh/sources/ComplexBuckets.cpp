#include <stdio.h>
#include <stdlib.h>
#include <map>
#include <set>
#include "headers.h"

void complexBuckets(dataT * data) {

/*    PointT ** sampleQueriesContext = generateSampleQueries(context_points, NULL),
      ** sampleQueriesChange = generateSampleQueries(change_points, NULL); 
      */  
    
}
