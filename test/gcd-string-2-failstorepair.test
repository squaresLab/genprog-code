#!/bin/sh

# remove any old repairs and run with configuration2
cd ../src/gcd-test-string && ../repair ../../test/gcd-string-2-failstorepair.config

# check to see if we've generated a repair, pass if we haven't
if grep "^No repair found." ../src/gcd-test-string/repair.debug.876552098
then
   exit 0
else
   exit 1
fi
~     
