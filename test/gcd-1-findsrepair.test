#!/bin/sh

# remove any old repairs and run with configuration2
rm ../src/gcd-test/repair.c
cd ../src/gcd-test && ../repair ../../test/gcd-1-findsrepair.config

# check to see if we've generated a repair, pass if we do
if [ -e ../src/gcd-test/repair.c ]
then
	exit 0
else 
	exit 1
fi
