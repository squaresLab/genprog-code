#!/bin/sh

# remove any old repairs and run with configuration2
cd ../src/gcd-test-string && ../repair ../../test/gcd-string-1-findsrepair.config

# check to see if we've generated a repair, pass if we do
if grep "^Repair Found: " ../src/gcd-test-string/repair.debug.1068139484
then
	exit 0
else
	exit 1
fi
